template(name="accountsHeaderButtons")
  li.account
    a.profile(href="{{pathFor route='currentUserProfile'}}")
      i.fa.fa-user
      span Profile
  li.account
    a.sign-out(href="#")
      i.fa.fa-sign-out
      span Sign Out


template(name="profile")
  h3 #{user.profile.name}
    
  if isCurrentUser
    p Edit Your Profile
    +quickForm schema=profileSchema doc=user.profile id="update-user-profile-form" template="bootstrap3"

  else
    h4 
      span 
        i Organization 
      span
        b #{user.profile.organization}
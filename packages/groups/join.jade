template(name="join")
  .container
    if invite
    
      button.btn.btn-primary.btn-large.join-button Join #{group.name}

    else
      .alert.alert-danger Invite is invalid or has already been used
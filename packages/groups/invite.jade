template(name="invite")
  .invite-user
    +autoForm(schema=inviteSchema id="inviteForm" type="method" meteormethod="invite")
      +afQuickField name="email"
      +afQuickField name="group" value=groupId
      .row
        .col-sm-4
          button.btn.btn-primary.btn-block(type="submit") Invite
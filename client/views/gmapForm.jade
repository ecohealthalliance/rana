template(name='gmapForm')

  +autoForm(
    collection='collections.Mappings'
    id='gmapForm'
    type='insert'
    validation='blur'
  )

    +afQuickField name='test'

    +afQuickField name='location'

    +afQuickField name='locationTwo'

    .btn-group
      button(class='gmap-submit', type='submit', class="btn btn-primary") Submit
      button(class='gmap-reset', type='reset', class="btn") Reset

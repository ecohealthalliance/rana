template(name="groupByControls")
  .panel.panel-default
    .panel-heading
      h3.panel-title Group By
    .panel-body
      select#group-by.form-control
        each groups
          if selected
            option(value=value selected) {{label}}
          else
            option(value=value) {{label}}